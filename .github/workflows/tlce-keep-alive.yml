name: Scheduled Keep-Alive Ping
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request
        env:
          TRANSLX_URL: ${{ secrets.TRANSLX_URL }}
        run: |
          if [ -z "$TRANSLX_URL" ]; then
            echo "❌ ERROR: Secret 'TRANSLX_URL' is not set"
            exit 1
          fi
          
          echo "🔄 Pinging URL..."
          curl -I -L -H "User-Agent: GitHubAction-KeepAlive" "$TRANSLX_URL"
          echo "✅ Ping successful"
